{
  "name": "parking-spaces",
  "version": "1.0.0",
  "private": true,
  "description": "Static Parking Spaces website prepared for Vercel deployment.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "lint": "node scripts/build-static.mjs --check",
    "test": "node scripts/build-static.mjs --check"
  },
  "engines": {
    "node": "24.x"
  }
}
